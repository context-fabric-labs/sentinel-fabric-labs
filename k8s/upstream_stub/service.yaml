apiVersion: v1
kind: Service
metadata:
  name: upstream-stub
  namespace: sentinel-fabric
  labels:
    app.kubernetes.io/name: upstream-stub
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: upstream-stub
